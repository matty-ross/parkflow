{% extends 'records/index.html.twig' %}

{% block main %}
    <h1>{{ 'title.record'|trans }}</h1>

    <div class="table-responsive">
        <table class="table">
            <tbody>
                <tr>
                    <th>{{ 'label.license_plate'|trans }}</th>
                    <td>{{ record.licensePlate }}</td>
                </tr>
                <tr>
                    <th>{{ 'label.recognized_user'|trans }}</th>
                    <td>
                        {% if record.recognizedUser %}
                            <a href="{{ path('app_users_show', {'id': record.recognizedUser.id}) }}">{{ record.recognizedUser }}</a>
                        {% endif %}
                    </td>
                </tr>
                <tr>
                    <th>{{ 'label.entered_at'|trans }}</th>
                    <td>{{ record.enteredAt|date('Y-m-d H:i:s') }}</td>
                </tr>
                <tr>
                    <th>{{ 'label.exited_at'|trans }}</th>
                    <td>{{ record.exitedAt ? record.exitedAt|date('Y-m-d H:i:s') : null }}</td>
                </tr>
            </tbody>
        </table>
    </div>

    <a href="{{ path('app_records_edit', {'id': record.id}) }}" class="btn btn-primary"><i class="bi bi-pencil"></i>{{ 'action.edit'|trans }}</a>
{% endblock %}
