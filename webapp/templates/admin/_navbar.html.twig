<div class="bg-body-tertiary">
    <nav class="navbar navbar-expand container">
        <a href="#" class="navbar-brand">ParkFlow</a>

        <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbar-content">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div id="navbar-content" class="navbar-collapse collapse">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a href="{{ path('app_admin_users_index') }}" class="nav-link"><i class="bi bi-person me-1"></i>{{ 'title.users'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_admin_vehicles_index') }}" class="nav-link"><i class="bi bi-car-front me-1"></i>{{ 'title.vehicles'|trans }}</a>
                </li>
            </ul>
        </div>

        <div class="dropdown">
            <button type="button" class="btn btn-secondary dropdown-toggle" data-bs-toggle="dropdown">
                <i class="bi bi-translate me-1"></i>{{ app.request.locale|locale_name(app.request.locale)|u.title }}
            </button>
            <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="#">{{ 'en'|locale_name('en')|u.title }}</a></li>
                <li><a class="dropdown-item" href="#">{{ 'sk'|locale_name('sk')|u.title }}</a></li>
            </ul>
        </div>
    </nav>
</div>
