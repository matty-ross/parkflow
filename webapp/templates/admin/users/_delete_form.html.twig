<form method="post" action="{{ path('app_admin_users_delete', {'id': user.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('app_admin_users_delete' ~ user.id) }}">
    <button type="submit" class="btn btn-danger">delete</button>
</form>
