{{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="reset" class="btn btn-secondary">{{ 'action.reset'|trans }}</button>
    <button type="submit" class="btn btn-primary">{{ 'action.submit'|trans }}</button>
{{ form_end(form) }}
